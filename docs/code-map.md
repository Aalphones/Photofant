# Code-Map — Photofant

> **Zweck:** Feature → Dateien als vertikale Slices über Frontend + Backend. Macht aus „wo ist Feature X" N Greps → einen Read. **Bewusst grob** (Ordner-/Modul-Ebene, keine Zeilennummern), damit sie Refactorings überlebt. Pflege: code-reference doc — bei Strukturänderung (neuer Feature-Ordner/Modul, verschobene Dateien) nachziehen.

## Navigations-Trick: die Namen laufen parallel

Frontend-Feature, Store-Slice, Service, Backend-API und Job heißen fast überall gleich. Wer ein Feature sucht, rät den Namen statt zu grep'en:

```
features/<x>/  ·  store/<x>/  ·  services/<x>.service.ts  ·  api/<x>.py  ·  jobs/<x>_job.py
```

Pfade unten relativ zu `frontend/src/app/` bzw. `backend/photofant/`.

## Kern: Galerie, Suche, Klassifizierung

| Feature | Frontend | Backend |
|---|---|---|
| **Galerie & Lightbox** | `features/galerie/` (galerie, grid[+`row-layout.ts` — pure Row-Breaking-Engine; `grid.ts` hostet `@tanstack/angular-virtual` row-level, flache `assets`-Liste statt Monatsgruppen, `#loadSentinel`/IntersectionObserver entfernt — P20], cell, face-grid[eigener Scroll-Container], lightbox[+zoom-stage, +`related-rail/` — generische „Ähnliche Bilder"-Kartenliste `{assetId, score, reasons}`, P36, ersetzt das alte Similar-Overlay; von P26 (Recommendation Engine) später mit befüllten `reasons` wiederverwendet, +`lore-panel/` — Wissen zum Bild/zur Person als weitere Panel-Sektion, 5 domänen-agnostische Sektionen (Kurzbio·Beziehungen·Franchises·Eigene Bilder·Quellen), Leer-Zustand „anlegen" → `/wissen`, P25 Phase 2], filter-rail, sub-toolbar — kein `edits`-Tab mehr seit P21) · `store/gallery/` (Entity-Key: `String(id)` für Assets, `` `v${version_id}` `` für Version-Pseudo-Einträge — P21 ADR-012), `store/filters/` · `ui/import-dialog/`, `ui/bulk-bar/` · `models/asset.model.ts`, `models/related-rail.model.ts` | `api/assets.py` (Stapel-Query: mischt `asset`- und `version`-Zeilen zu flachen Einzeleinträgen mit `stack_size`/`stack_group_id`, P21 ADR-012) · `jobs/import_job.py`, `jobs/thumbnail_job.py` · `media/thumbnails.py`, `media/moves.py`, `media/meta.py` · `api/review.py` (`GET /assets/{id}/similar` — nur noch MCP-Aufrufer, kein Frontend-Caller mehr seit P36 Phase 3) |
| **Suche** (Freitext exakt/Prefix, Tag, Caption, semantisch, Reverse-Image) | `ui/search-box/` (Drop-Zone + Bild-Upload → Reverse; expliziter Semantik-Umschalter mit Tooltip, P36 Phase 4 — Quelle der Wahrheit ist `store/search`s `mode`, lokal nur `pendingSemanticToggle` für „getoggelt, noch kein Text") · `store/search/` · `store/filters/` (`reverseSearch` = exklusiver Reverse-Filter-Modus, P36) · `store/gallery/gallery.effects.ts` (Ladefehler → deutsche Meldung via `extractApiErrorMessage`, angezeigt in `features/galerie/galerie.ts` als Toast, P36 Phase 4) · `services/search.service.ts` (`by-image`, `semanticByAsset` — Lightbox-Rail + „mehr"-Sprung, P36) · `services/api-error.util.ts` (`extractApiErrorMessage` — geteilter Backend-Fehlertext-Parser, P36 Phase 4) · `models/search.model.ts` | `api/assets.py` (`list_assets`, alle `q_mode`-Zweige; `q_mode=text` = Tag-/Personen-Name ILIKE + Caption via FTS5, Fuzzy-Toleranz entfernt, ADR-015-Nachtrag; `q_mode=semantic` bleibt bewusst der einzige Frontend-Pfad der Text-Semantiksuche, P36 Phase 4 — volle Paginierung/Facetten statt Doppel-Roundtrip über `/api/search/semantic`; `similar_ids`-Param sortiert nach vorgegebener Reihenfolge, P36) · `db/text_index.py` (FTS5-Caption-Index `asset_caption_fts`) · `api/search.py` (`POST /api/search/semantic` — nur `like_asset_id`-Zweig hat einen Frontend-Aufrufer (Lightbox-Rail + „mehr"), P36; `query`-Zweig bleibt bewusst totes Backend-Duplikat, siehe `docs/routes.md`; `POST /api/search/by-image` — Upload-Embed für Reverse Image Search, P36) · `db/vector_index.py` · `jobs/embedding_job.py` · `inference/image_embedder.py` (Capability-Resolver `resolve_image_embedder`, Rollen `semantic_search`/`visual_rerank`, ADR-022) · `inference/adapters/clip.py` · `inference/adapters/siglip.py` · `inference/adapters/dinov2.py` (image-only Re-Ranking-Embedder, 768-dim, P37/ADR-023) · `search/rerank.py` (DINOv2 Two-Stage-Rerank für Bild→Bild, P37/ADR-024 — reiner `_rank_by_cosine` + `rerank_by_appearance`; hängt in `like_asset_id` + `by-image` von `api/search.py`; `vector_index.load_dino_embeddings` liest die Kandidaten-Vektoren) |
| **Tags** | `features/einstellungen/tags/` · `store/tags/` · `services/tag.service.ts` | `api/tags.py` · `jobs/tagging_job.py` · `inference/adapters/wd14.py` |
| **Captions & Presets** | `ui/caption-preset-form/`, `ui/preset-dialog/` · `store/presets/` · `services/caption-preset.service.ts` · `models/caption-preset.model.ts` | `api/caption_presets.py` · `jobs/caption_job.py` · `inference/caption_config.py` · `inference/adapters/{florence2,joycaption,qwen_vl}.py` |
| **Klassifizierung & Heuristik** (P18) | `services/classify.service.ts` · `features/einstellungen/klassifizierung/` (Shell + Child `kategorie-editor/` — Kategorien/Labels-CRUD, Modus-Umschalter, Seed-Katalog editierbar) · `store/classification/` (Entity-Store der Kategorien, `classification.effects.ts` ruft `services/classification.service.ts`) · `models/classification.model.ts` (`ClassificationCategory`, `ClassificationLabel`, `AssetClassification`) · Lightbox-Sektion + Galerie-Filter-Rail-Gruppe je Kategorie: `features/galerie/lightbox/`, `features/galerie/filter-rail/` | `api/classify.py` (Rerun-Step `categories`) · `api/classification.py` (CRUD Kategorien/Labels, explizites Cascade-Delete — `PRAGMA foreign_keys` projektweit aus) · `api/assets.py` (`classification`-Filter OR/AND, Facet `classifications`, `AssetDetailDto.classifications`, Label-Treffer im `q_mode=text`) · `jobs/heuristics_job.py` · `classification/engine.py` (CLIP+WD14-Fusion, kein Modell-Neulauf), `classification/scoring.py`, `classification/seed.py` (Konzept-Seed-Katalog) · `jobs/classification_job.py` (ein Asset, persistiert + setzt `ProcessingLedger.classified`), `jobs/classification_pipeline.py` (wartet auf Tagging+Embedding, enqueued dann `classification_job`) |
| **Alben & Smart-Alben** | `features/alben/` (alben, album-settings) · `store/collections/` · `services/collection.service.ts` · `models/collection.model.ts` | `api/collections.py` · `collections/engine.py` · `jobs/collections_job.py` |

## Personen, Faces, Review

| Feature | Frontend | Backend |
|---|---|---|
| **Personen & Faces** | `features/personen/` (personen, person-card[+„🆕 Neue Person"-Inline-Banner, P24 Phase 2 — offene `new_person`-Aufgabe aus `store/knowledge` pro Person, 3 Aktionen anlegen/später/ignorieren, „anlegen" öffnet den P23-`entity-wizard-dialog` wieder, danach `PersonService.linkEntity` + Task-Resolve], merge/split/dupe-check/delete-person-dialog, alphabet-rail, group-color.util.ts) · `features/galerie/face-cell/`, `face-grid/` · `store/persons/`, `store/knowledge/` (P24: Task-Queue-Anbindung für die Affordance) · `services/person.service.ts` (`linkEntity`, P24) · `models/person.model.ts` | `api/faces.py`, `api/persons.py` (`POST/DELETE /{id}/link-entity`, P24) · `api/assets.py` (`PATCH /assets/{id}/assign-person`, P30 — Person ohne Face zuordnen) · `jobs/face_job.py`, `jobs/clustering_job.py` · `clustering/engine.py` · `inference/adapters/buffalo_l.py` · `db/face_vector_index.py` · `media/person_folders.py` (inkl. `delete_person()`, `_resolve_person_smart_triggers()`) |
| **Review-Queue** (Faces + Dupes) | `features/review/` (review, review-faces, review-dupes[+dupe-compare, dupe-pair-row]) · `store/review/` · `services/review.service.ts` · `models/review.model.ts` | `api/review.py`, `api/review_queue.py`, `api/duplicates.py` · `jobs/dupe_scan_job.py` (DINOv2 seit P37 Phase 4, ADR-024; vorher CLIP-only, ADR-018) |

## Editor & Generativ

| Feature | Frontend | Backend |
|---|---|---|
| **Editor** (CPU: Crop/Rotate/rembg) | `features/editor/` (editor, basis-panel, crop-overlay, mask-overlay, step-bar, save-modal) · `store/editor/` · `services/edit-session.service.ts` · `models/edit-session.model.ts` · `ui/bulk-edit-dialog/` | `api/edit_sessions.py` · `jobs/bulk_edit_job.py` · `media/ops.py` · `media/orientation_overwrite.py` (reine Orientierungs-Sessions rotate/mirror überschreiben Quelle statt neue Version, P-editor-basis-fixes Phase 3) |
| **Generativ** (Upscale/Edit/Inpaint via ComfyUI) | `features/editor/` (flux2-panel = Edit, inpaint-panel, upscale-panel, resolution-field) · `store/editor/` (runGenerative) · `features/galerie/run-leiste/` (Prompt/Resolution) · Bulk-Upscale: `ui/bulk-bar/` + Galerie · Einzel-Upscale: `galerie/lightbox/`. Default-Aktionen (Edit/Inpaint/Upscale) über `services/comfyui.service.ts` (`runDefaultWorkflow` → Auto-Import); Run-Leiste über `runWorkflow` (Fire-and-forget bleibt) | `api/comfyui.py` · `jobs/comfyui_run_job.py` · `inference/generative_engine.py` (nur heavy_captioners) |
| **ComfyUI-Integration** | `features/galerie/run-leiste/` · `features/einstellungen/comfyui/` · `ui/comfyui-import-dialog/` · `store/comfyui/`, `store/prompt-templates/` · `services/comfyui.service.ts`, `prompt-template.service.ts` · `services/run-draft.service.ts` (Run-Leisten-Entwurf tab-übergreifend: Slot-Belegungen/Workflow/Toggles leben hier statt in `galerie.ts`, Live-Sync über `BroadcastChannel` + `localStorage`; „scharfer" Slot bleibt lokal in `galerie.ts`) · `models/comfyui-workflow.model.ts`, `prompt-template.model.ts` | `api/comfyui.py`, `api/prompt_templates.py` · `jobs/comfyui_run_job.py` · `comfyui/` (client, introspect, validator) · Workflow-Discovery: `.photofant/workflows/*.json` (Dateiname = key, kein Upload/DB) — 3 Default-Zuordnungen (Upscale/Edit/Inpaint) in `settings.json`; Default-Run importiert kuratierte Ergebnisse automatisch, generischer Run bleibt Fire-and-forget |

## Verwaltung

| Feature | Frontend | Backend |
|---|---|---|
| **Modell-Management** | `features/modelle/` (modelle, bind-dialog, download-dialog, model-card, model-drawer) · `store/models/` · `services/model.service.ts` · `models/model.model.ts` | `api/models.py` · `models/loader.py`, `models/validation.py`, `models/vram.py` · `jobs/download_job.py` · `inference/session_manager.py` |
| **Wartung & Datensicherheit** | `features/wartung/` (FS↔DB-Reconcile, Thumbnail-Rebuilds, Bild-Re-Embed, Personen-Clustering, Backup — eine Seite, kein Einstellungen-Tab) · `features/review/review-reconcile/` (Reconcile-Report-UI, Shell + generische `rr-section`-Child) · `store/maintenance/` · `services/maintenance.service.ts` · `services/classify.service.ts` (Re-Embed nutzt `POST /api/classify/rerun` mit `asset_ids:"all"`) · `models/maintenance.model.ts` | `api/maintenance.py` · `api/classify.py` · `maintenance/` (reconcile, repair, store) · `jobs/{rebuild,reconcile,backup,rerun}_job.py` |
| **Papierkorb & Favoriten** | `features/papierkorb/`, `features/favoriten/` · `store/trash/` | `api/trash.py` · `media/moves.py` (Favorit = physischer Move) |
| **Einstellungen** | `features/einstellungen/` (Shell + darstellung, bibliothek, verarbeitung, bearbeitung, info, tastaturkuerzel, tags, klassifizierung, comfyui, mcp) · `services/settings.service.ts` · `models/config.model.ts` | `api/config.py` · `settings.py`, `config.py` |
| **MCP-Schnittstelle** (Agent-Steuerung, ADR-019) | `features/einstellungen/mcp/` · `store/mcp/` · `services/mcp.service.ts` (liest/schreibt den `mcp`-Block über das generische `/api/config`) · `models/config.model.ts` (`McpConfig`) | `mcp/` (`server.py` = FastMCP-Instanz + `/mcp`-Mount + Flag-Guard/Loopback-Middleware, `adapter.py` = `run_endpoint()` DB-Session-Brücke zu `api/*.py`, `gate.py` = Confirmation-Gate, `tools/` = ein Modul je Phase) · Mount in `main.py` (`mount_mcp`, Session-Manager im `_lifespan`) · `settings.py` (`mcp`-Block) |
| **Trainingssets & Export** | `features/trainingssets/` (trainingssets, training-set-item, training-set-settings, training-set-stats, training-set-captions, training-set-dupes, training-set-export) · `ui/export-dialog/` (Galerie + Favoriten, gemeinsam) · `store/collections/` · `services/collection.service.ts`, `export.service.ts` | `api/collections.py` (`/items`, `/stats`, `/export`, `/captions`, `/duplicates`) · `api/export.py` (Favoriten-Exporte) · `jobs/export_job.py` (Sidecar-Writer, Train/Val-Split) · `collections/stats.py` (AR-Buckets, Near-Dupe-Quote) |
| **Wissensbasis** (P22, ADR-025 — Markdown = Wahrheit, SQLite = Cache; Frontend-Kontrakt seit P23 komplett; Lore-API-Vollform seit P25 Phase 1; Korrektur-Flow seit P25 Phase 3 — **P25 komplett, MVP erreicht**) | `models/knowledge.model.ts` (Entity/Domain/Task-DTOs + `PatchEntityRequest`/`PatchJobResponse`/`ChangelogEntryDto`, P25 Phase 3) · `store/knowledge/` (Domänen-Liste + Tasks-Liste als je eigener EntityAdapter im selben Feature-State, Create-Entity-Status) · `services/knowledge.service.ts` (`listDomains`, `listEntities`, `searchEntities`, `createEntity`, `updateEntity`, `getLore` [P25 Phase 2], `listTasks`, `resolveTask`, `dismissTask`, `patchEntity`/`getChangelog` [P25 Phase 3]) · `store/knowledge/` (dritter EntityAdapter `entityList` für die Übersichtsliste, gleiche Doppelnutzung als by-id-Lookup fürs Wizard-Edit-Prefill; `lastCreatedEntity`/`lastUpdatedEntity` steuern den Erfolgs-Banner, „Nachtrag Entity-Liste + Erledigen-Edit-Modus") · `features/wissen/` (Seite mit „Neue Entity"-CTA + Übersichtsliste aller Entities [Zeile anklicken → Wizard im Edit-Modus] + `entity-wizard-dialog/` — Pflicht Typ+Titel, eingeklappt Domäne/Aliase/Beschreibung/Beziehungen-Autocomplete, `prefill`-Input für Aufgaben-Vorbelegung, `editEntity`-Input für Bearbeiten-Modus [Typ/Domäne gesperrt, `update`-Output statt `save`] + `work-queue/` — offene Aufgaben, „Erledigen" öffnet den Wizard vorbelegt [`incomplete_entity` → Edit-Modus, sonst Anlage-Modus] und löst die Aufgabe nach dem Speichern auf, „Später"/„Ignorieren" als Sekundäraktion, „Nachtrag Entity-Liste + Erledigen-Edit-Modus") · Nav-Eintrag „Wissen" in `shell/nav-rail/`, Route `wissen` in `app.routes.ts` · **Lore-Panel-UI (P25 Phase 2)**: `features/galerie/lightbox/lore-panel/` (`pf-lore-panel`) verbraucht `getLore` und rendert das Wissen als weitere Sektion in P15s Lightbox-Panel; Beziehungs-/Franchise-Klick → `/wissen?entity=`, Leer-Zustand → `/wissen`; **Korrektur-Flow (P25 Phase 3)**: „Das stimmt nicht" bei auto/inferred-Kurzbio (`owner !== 'user'`) öffnet ein Inline-Formular (Wert + Grund), löst `patchEntity` aus und wartet über `JobsService.streamJobs()` auf den `KnowledgePatchJob` (done/error), dann Lore-Reload ohne Lightbox-Reload (gleiches SSE-Warte-Muster wie `editor.effects.ts::onRunGenerativePoll$`) | `knowledge/` (`schema.py` = Entity/Relationship/Owner-Datenmodell, inkl. `body` (freier Markdown-Text) · `parser.py` = Markdown↔Entity, verlustfreier Round-Trip via `python-frontmatter` · `validator.py` = Frontmatter-Regeln gegen Domäne · `domains.py` + `domains/movies.yaml` = erlaubte Typen, domänen-agnostisch · `vault.py` = Ordnerstruktur + Datei-I/O, `open_vault()`, `list_domains()` (alle Domänen-YAMLs, P23 Phase 2 — für den Wizard-Typ-Dropdown); seedet einmalig `AGENTS.md` (`AGENTS.md.template`) in die Vault-Wurzel — Anleitung für Agenten, die direkt im Vault-Ordner arbeiten (Schema, neue Domäne/Entity anlegen, Cache-Sync-Hinweis); `iter_entity_files()` überspringt Root-Level-`.md`-Dateien (Entities liegen immer in `<type-folder>/<slug>.md`) · `repository.py` = `EntityRepository`/`RelationshipRepository`, SQLite-Cache-Lesepfad über `knowledge_*`-Tabellen, inkl. `EntityRepository.get_many()` (Bulk-Auflösung für Lore-Beziehungsziele, P25) · `service.py` = `KnowledgeService`, einzige Mutationsschicht (Ownership-Durchsetzung, Markdown-first, P22 Phase 3; `body` patchbar seit P23 Phase 2, öffentliche `PATCHABLE_FIELDS`-Konstante seit P25 Phase 3); `get_lore()`/`get_lore_for_media()` seit P25 Phase 1 Vollform (aufgelöste Beziehungsziele, Franchises, Quellen, rohe Media-Links — Medien-Join lebt in `api/knowledge.py`, damit dieses Modul frei von Person-/Asset-/Face-Importen bleibt) · `changelog.py` = `ChangelogService` (P25 Phase 3) — Explainability-Log der Korrekturen über `knowledge_changelog`, Arbeitszustand/Metadaten wie `tasks.py`, kein Vault-Wissen · `maintenance.py` = `rebuild_cache`/`reconcile_cache`, Cache aus dem Vault neu aufbauen/abgleichen (P22 Phase 4) · `tasks.py` = `TaskService`, Aufgaben-Queue „hier fehlt Wissen" — Arbeitszustand über `knowledge_tasks`, Dedup über `kind`+`context`, P23 Phase 1; `TaskKind.INCOMPLETE_ENTITY` seit „Nachtrag Entity-Liste + Erledigen-Edit-Modus" — `KnowledgeService._flag_if_incomplete()` legt sie bei `create_entity` an, wenn `body` und `relationships` beide leer sind) · `api/knowledge.py` (REST-CRUD + Beziehungen + `GET /domains` [P23 Phase 2, nicht im P22-Kontrakt] + `body`-Feld in Entity-DTOs; Lore-Vollform seit P25 Phase 1: `GET /entities/{id}/lore` [entity-id-basiert, 404] + neu `GET /lore?asset_id=/person_id=` [P25-Kontrakt, 200 mit `entity: null` ohne Verknüpfung] — beide über `LoreDto`, Medien-Join für „Eigene Bilder"-Thumbnails via `_resolve_media_refs`; Korrektur-Flow seit P25 Phase 3: `POST /entities/{id}/patch` [löst `KnowledgePatchJob` asynchron aus] + `GET /entities/{id}/changelog` [Explainability-Historie]) · `api/knowledge_tasks.py` (Aufgaben-CRUD/Statuswechsel + `POST /lookup`-Trigger, P23 Phase 1) · `jobs/knowledge_lookup_job.py` (`KnowledgeLookupJob` — legt bei fehlender Entity eine Aufgabe an, ohne KI; Auto-Trigger aus Ereignissen ab P24) · `jobs/knowledge_patch_job.py` (`KnowledgePatchJob`, P25 Phase 3 — Einzelfeld-Patch über `KnowledgeService.update_entity` + Changelog-Eintrag, Sackgassen-Job wie `KnowledgeLookupJob`; `owner`-Parameter offen für P27s KI-Korrekturvorschläge, die denselben Pfad wiederverwenden) · Registrierung beider Router in `main.py` · `settings.py` (`knowledge`-Block: `vault_path`, `default_domain`) · **Wartungs-Auslösung** hängt an `jobs/rebuild_job.py` (`RebuildTarget` `knowledge`/`knowledge_reconcile`) + `features/wartung/`-Karte „Wissensbasis", kein eigener Job/Screen |
| **KI-Layer / Gemma** (P27, ADR-027/028 — Jobs fordern Fähigkeiten, nicht Modelle; Gemma über torch/transformers; **Phase 1: Backend-Layer · Phase 2: KI-Vorschlag im Wizard (Import) · Phase 3: KI-Ergänzung im Lore Panel (Update) · Phase 4: Interview-Mode für private Personen**) | **Phase 2:** `features/wissen/entity-wizard-dialog/` („KI-Vorschlag"-Button beim Anlegen — nur wenn `importAutonomy !== 'off'`; belegt Beschreibung/Aliase/Beziehungen als bestätigungspflichtigen Vorschlag vor, zeigt Explainability [Modell·Prompt-Version·Konfidenz·Grund] + Validierungs-Abweisung; neue Inputs `importAutonomy`/`suggestionLoading`/`suggestionResult`/`suggestionError`, Output `requestSuggestion`) · `features/wissen/wissen.ts` (lädt Autonomie, reicht Vorschlags-Signale an den Wizard, `resetImportSuggestion` beim Öffnen/Schließen) · `store/knowledge/` (Autonomie + Vorschlags-Status; `correlateSuggestionJob$`-Effekt fischt das Job-Ergebnis aus dem Job-Stream) · `services/knowledge.service.ts` (`getAiAutonomy`, `requestImportSuggestion`) · `models/knowledge.model.ts` (`AiAutonomyDto`, `ImportSuggestionRequest/Response`, `KnowledgeImportResult/Suggestion/Explainability`) · `models/job.model.ts` (`Job.result` generisches Ergebnis-Feld, `'knowledge_import'`-Kind) · **Phase 3:** `features/galerie/lightbox/lore-panel/lore-panel.ts` (self-contained, kein Store-Umweg — lädt `getAiAutonomy()` direkt im Konstruktor; „Ergänzen (KI)" neben „Das stimmt nicht" [gleiche Ownership-Bedingung `canCorrectFor`, zusätzlich `ai.autonomy.knowledge_update !== 'off'`], löst `requestUpdateSuggestion` aus und wartet über `JobsService.streamJobs()` auf den `KnowledgeUpdateJob` [gleiches SSE-Warte-Muster wie der P25-Korrektur-Flow]; Ergebnis zeigt Alt/Neu-Diff + Begründung + Konfidenz; „Annehmen" ruft `acceptUpdateSuggestion` [`owner=inferred`, eigener Pfad — **nicht** `patchEntity`, das ist fix `owner=user`], „Ablehnen" verwirft nur lokal, kein Backend-Call) · `services/knowledge.service.ts` (`requestUpdateSuggestion`, `acceptUpdateSuggestion`) · `models/knowledge.model.ts` (`UpdateSuggestionRequest/Response`, `KnowledgeUpdateResult/Proposal/Explainability`, `AcceptUpdateSuggestionRequest/Response`) · `models/job.model.ts` (`'knowledge_update'`-Kind) · **Phase 4:** `features/wissen/interview-dialog/` (`pf-interview-dialog` — eigener Modal-Dialog neben dem Wizard; festes Fragen-Skript Schritt-für-Schritt [Intro Name+Typ → Fragen → Synthese-Warteansicht → Zusammenfassung], kein freies Chat; „Anlegen" emittiert eine normale `CreateEntityRequest` mit `owner=user`) · `features/wissen/wissen.ts`+`.html` (Button „Private Person (Interview)" neben „Neue Entity", nur wenn `interview !== 'off'` UND eine private Domäne existiert; `showInterview`-Signal, Erfolgs-Effekt schließt beide Dialoge) · `store/knowledge/` (`requestInterview`/`interviewReady`/`interviewFailed`/`resetInterview`; `correlateInterviewJob$` fischt das Interview-Ergebnis aus dem Job-Stream — Zwilling von `correlateSuggestionJob$`) · `services/knowledge.service.ts` (`requestInterviewSynthesis`) · `models/knowledge.model.ts` (`InterviewAnswer`, `InterviewSynthesizeRequest/Response`, `KnowledgeInterviewResult/Suggestion/Explainability`; `DomainDto.private`) · `models/job.model.ts` (`'interview'`-Kind) | `inference/capabilities.py` (`Capability`-Enum + Resolver `resolve_generator`/`generate` → `GenerationResult`-Explainability-Payload; `autonomy_for` liest `ai.autonomy`; Capability→Modell über `ai.capabilityMap`, Default `ai.gemmaModel`) · `inference/tools.py` (`ToolRegistry` — gefencte Tools `ReadMarkdown`/`SearchKnowledge`/`ValidatePatch`/`PatchEntity` über `KnowledgeService`; `INTERVIEW` ohne Such-/Lese-Tool, ADR-009; noch ungenutzt — Import-/Update-Job rufen `generate()`/`KnowledgeService` direkt, kein Tool-Routing) · `inference/adapters/gemma.py` (`GemmaAdapter`/`resolve_gemma` — lädt über `generative_engine.load_transformers_model(load_processor=False)`, `TextGenerator`-Protokoll) · `inference/adapters/gemma_gguf.py` (`GemmaGgufAdapter`/`GemmaGgufVisionAdapter`/`resolve_gemma_gguf` — 2. Runtime via llama.cpp, P35/ADR-029; `resolve_generator` routet nach Manifest-`format`) · `inference/interfaces.py` (`VisionTextGenerator` — Vision-Naht, mmproj optional ladbar, strukturell per `isinstance` geprüft, noch kein Konsument) · `inference/prompt_library.py` (versionierte Markdown-Prompts, `ai.promptLibraryPath` sonst mitgelieferte `inference/prompts/*.md`) · `inference/generative_engine.py` (`load_processor`-Schalter für Text-LM, `evict_idle`-Timeout-Override) · `inference/interfaces.py` (`TextGenerator`) · `knowledge/service.py` (`validate_patch` = Trockenlauf ohne Schreiben) · `settings.py` (`ai`-Block) · `models/manifest.json` (`gemma-3-4b-it`, Rolle `text_generator`) · `main.py` (Idle-Loop reicht `ai.idleTimeoutSeconds` durch) · **Phase 2:** `jobs/knowledge_import_job.py` (`KnowledgeImportJob` — Gemma erzeugt aus Kontext einen Beschreibungs-Kandidaten, Validator-Trockenlauf, Ergebnis über den Job-Stream; Sackgassen-Job) · `api/knowledge_ai.py` (`GET /knowledge/ai/autonomy` + `POST /knowledge/ai/import-suggestion` — Autonomie-Gate, löst den Import-Job aus) · `jobs/queue.py` + `api/jobs.py` (`JobStatus.result`/`set_result` + `JobDto.result` — Jobs können ein strukturiertes Ergebnis über den Stream liefern) · **Phase 3:** `jobs/knowledge_update_job.py` (`KnowledgeUpdateJob` — Gemma überarbeitet die bestehende Beschreibung einer Entity, Validator-Trockenlauf über `KnowledgeService.validate_patch`, Ergebnis [`proposal`/`old_body`/Explainability/Validierungsfehler] über den Job-Stream; Sackgassen-Job wie `KnowledgeImportJob`) · `api/knowledge_ai.py` (`POST /knowledge/ai/update-suggestion` [löst den Update-Job aus] + `POST /knowledge/ai/update-suggestion/accept` [übernimmt über `enqueue_knowledge_patch` mit `owner=inferred`, eigene Route statt der user-fixen `/patch`], beide mit Autonomie-Gate) · **Phase 4:** `jobs/interview_job.py` (`InterviewJob` — Gemma synthetisiert aus den Interview-Antworten einen Beschreibungsvorschlag für eine private Person/ein Haustier, Validator-Trockenlauf, Ergebnis über den Job-Stream; nur Zusammenfassung, nie Faktenerfindung — Konzept-ADR-009; Sackgassen-Job) · `api/knowledge_ai.py` (`POST /knowledge/ai/interview` [Autonomie-Gate + Guard: Domäne muss privat sein] + Guard in `import-suggestion`: private Domäne → 422; Helper `_is_private_domain`) · `knowledge/domains.py` (`Domain.private`-Flag, aus `private: true` in der YAML) · `knowledge/domains/private.yaml` (mitgelieferte private Domäne, Typen Person/Pet — wird wie `movies.yaml` in den Vault geseedet) · `api/knowledge.py` (`DomainDto.private` durchgereicht) · `inference/prompts/interview.md` (v2 — Synthese-Prompt statt Live-Interview) · `jobs/queue.py` (`JobKind.INTERVIEW`) |
| **Empfehlungen** (P26, ADR-026 — CLIP-Ähnlichkeit + Wissensgraph, kein neues Modell — **komplett**: Backend, Karten-UI unter dem Lore-Panel, Explainability; Graph-Empfehlungen + reine CLIP-Ähnlichkeit seit Post-P26-Cleanup in **einer** Liste, keine zweite „Ähnliche Bilder"-Sektion mehr) | `models/recommendation.model.ts` (`RecommendationDto`/`RecommendationsResponse`/`WhyNotResponse`, `recommendationReasonLabel()`/`recommendationMissingLabel()` — mappen `{signal, detail}` auf „✓ gleiche Person (…)"/„✗ gleiche Rolle (fehlt)"; `WhyNotResponse`/`whyNot()` bleiben als Service-Methode für spätere/andere Konsumenten bestehen, ohne aktuellen UI-Caller) · `models/explainability.model.ts` (`ExplainabilityPayload` — geteiltes `{title, confidencePercent, reasons[], missing[], meta[]}`, auch von der P25-Korrektur-Historie genutzt) · `ui/explainability-popover/` (`pf-explainability-popover` — Trigger-Icon + `position: fixed`-Panel, `z-index: 500` über allen Lightbox-Dialogen, Hintergrund `--surface-2`; kein eigener HTTP-Call, `open`/`payload`/`loading` vom Aufrufer gesteuert) · `services/recommendation.service.ts` (`getRecommendations`, `whyNot`) · `features/galerie/lightbox/lightbox.ts` (`recommendations`-Signal [Graph, cache-backed] + `relatedRail`-Signal [reine CLIP-Nachbarschaft, P36] laufen weiter parallel an derselben Reload-Pipeline wie `detail`, werden aber in `mergedRecommendationItems` dedupliziert zu einer Liste zusammengeführt — Graph-Empfehlungen zuerst, dann CLIP-Treffer ohne Graph-Signal; „Mehr ähnliche Bilder" sitzt jetzt in der Empfehlungen-Sektion, Sichtbarkeit über `hasEmbedding()`; „Warum?"-Payload synchron aus Score/Reasons, für reine CLIP-Treffer schlanker Fallback ohne Reason-Chain) rendert die generische `features/galerie/lightbox/related-rail/` (`{assetId, score, reasons}`, P36; optionale `explainLabel`/`explainOpenId`/`explainPayload`/`explainLoading`-Inputs + `explainOpen`/`explainClose`-Outputs fürs Popover pro Karte) einmalig direkt unter `pf-lore-panel` · `features/galerie/lightbox/lore-panel/lore-panel.ts` („Warum geändert?"-Popover neben der Kurzbio, sobald `owner === 'user'`, gespeist aus `getChangelog`) · `models/job.model.ts` (`JOB_KINDS` um `'recommendation'` ergänzt) | `recommendation/` (`context.py` = Graph-Kontext eines Assets — `persons` (aktive `asset_instance`, ohne `_unknown`), `roles` (direkt verknüpfte Entities), `films` (deren 1-Hop-Beziehungsziele); gebündelte Bulk-Auflösung + Kandidatensammlung · `scoring.py` = `Reason`/`ScoredRecommendation`/`Weights`, `score_pair` (gewichtete Summe same_person/same_role/same_film/clip + Reason-Chain), `compute_recommendations` (CLIP-Nachbarn via `db/vector_index.py` **+** Graph-Kandidaten → Score → Schwelle → Top-N), `pair_clip_similarity` für „Warum nicht?") · `jobs/recommendation_job.py` (`RecommendationJob` — rechnet + ersetzt `recommendation_cache` idempotent, Sackgassen-Job; ein Job bedient „auf Abruf" und „Refresh"; `invalidate_recommendations` löscht Cache-Zeilen gezielt bei Graph-Änderungen, ADR-030, Call-Sites in Person-/Wissensgraph-/Clustering-Mutationen) · `api/recommendations.py` (`GET /recommendations?asset_id=` aus Cache, Fehltreffer plant den Job [nie synchron] · `GET /{source}/{target}/why-not` live, ohne aktuellen Frontend-Caller) · `db/models.py` (`Recommendation` = `recommendation_cache`, ohne `job_id`-Spalte) · `settings.py` (`recommendations`-Block: `enabled`, `max_results`, `min_score`, `weights`) |

## Querschnitt (überall benutzt)

| Bereich | Frontend | Backend |
|---|---|---|
| **App-Shell & Auth** | `app.component.ts`, `app.config.ts`, `app.routes.ts` · `shell/` (shell, nav-rail, top-bar) · `guards/auth.guard.ts` · `features/unlock/` · `services/auth.service.ts` | `main.py` · `api/auth.py`, `api/health.py`, `api/info.py` |
| **Jobs / Queue** (alles Langsame) | `ui/job-dock/`, `ui/job-pill/`, `ui/rerun-dialog/` · `store/jobs/` · `services/jobs.service.ts` · `models/job.model.ts` | `api/jobs.py` · `jobs/queue.py` · `jobs/rerun_job.py` |
| **DB & Persistenz** | — | `db/` (engine, session, models, cache, vector_index, face_vector_index) · `alembic/versions/` (Migrationen) |
| **Inferenz-Infra** | `ui/gated-feature/` (Feature-Gating) | `inference/` (interfaces, preprocessing, session_manager, generative_engine [torch], gguf_engine [llama.cpp, 2. Runtime]; KI-Layer: capabilities, tools, prompt_library, adapters/{gemma,gemma_gguf}) · `models/loader.py` |
| **UI-Bausteine** | `ui/icon/`, `ui/shortcut-legend/` · `services/shortcut.service.ts` | — |

## Wo was wohnt (Faustregeln)

- **Eine HTTP-Route suchen?** → `backend/photofant/api/<feature>.py`, gespiegelt in `docs/routes.md`.
- **Ein DB-Feld suchen?** → `backend/photofant/db/models.py`, gespiegelt in `docs/models.md`.
- **„Langsam, aber blockiert die UI nicht"?** → es läuft als Job: `backend/photofant/jobs/<x>_job.py`, orchestriert von `jobs/queue.py`. Die Queue hat mehrere Spuren: parallel (Downloads, fire-and-forget) · Main-Worker FIFO (user-getriggert: Import, Export, ComfyUI-Run, Scan, Backup …) · Background-Worker mit Prioritäts-Queue (Hintergrund-Inferenz: Face vor Embedding/Heuristics/Dupe-Scan — `_BACKGROUND_PRIORITY` in `queue.py`) · dedizierte Tagging- und Captioning-Worker, je in konfigurierbarer Anzahl (Settings `tagging_workers`/`captioning_workers`, Default 1) statt fix einem Worker, damit sie auf der GPU überlappen können ohne sich eine ONNX-Session zu teilen. So warten schnelle user-Jobs nie hinter einem langsamen Caption-Lauf.
- **ML-Modell-Adapter?** → `backend/photofant/inference/adapters/`.
- **NgRx-State zu einem Feature?** → `frontend/src/app/store/<feature>/`.
