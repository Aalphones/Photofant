# FINDINGS — P33 pHash-Ablösung

> Erkenntnisse während der Umsetzung, getaggt auf die Phase, die sie betreffen.
> Format: `- [ ] → Phase N: <Erkenntnis, 1-3 Zeilen — was wurde entdeckt, was heißt das für die Ziel-Phase>`

- [x] → Phase 2: `dupe_threshold` in `settings.py` bleibt entgegen Phase-1-AK-4 bewusst erhalten — `api/collections.py` `/duplicates` (Trainingsset-pHash-Suche, Fence-Funktion #3) liest ihn noch und würde sonst mit KeyError crashen. Beim Umbau dieses Endpoints auf CLIP in Phase 2 den Key final aus Dataclass/Defaults/`_EXPECTED_TYPES` entfernen. → erledigt: `dupe_threshold` komplett aus `settings.py` (+ `settings.example.json`) entfernt.
- [x] → Phase 4: **Nicht mehr zu tun** — `api/assets.py`: `AssetDto.has_phash` (`asset.phash is not None`) war ein stiller Rest-Carrier: seit Phase 1/2 lieferte er auf jedem neu importierten Bild `false` (Embedding-Job schreibt kein `phash` mehr), wodurch der „Ähnliche Bilder"-Button im Lightbox seit Phase 1 für alle neuen Assets unbemerkt deaktiviert war. Kontrakt nannte das Feld nicht explizit, aber Phase 3s eigene AK „kein phash-Vorkommen mehr unter frontend/src/" erforderte die Umbenennung im Frontend-Modell — dafür musste das Backend-Feld mit. Bereits in Phase 3 behoben: `has_phash` → `has_embedding` (`asset.clip_embedding is not None`), inkl. Batch-Query in `list_assets` (verhindert N+1 auf der deferred BLOB-Spalte). Phase 4s Grep-Sweep über `backend/photofant/` wird hier bereits 0 Treffer zeigen — keine weitere Aktion nötig.
