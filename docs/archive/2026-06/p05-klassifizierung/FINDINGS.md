# FINDINGS — P5 Klassifizierung

> Erkenntnisse während der Umsetzung, getaggt auf die Phase, die sie betreffen. Format:
> `- [ ] → Phase N: <Erkenntnis>`

- [x] → Phase 5: Caption läuft bisher **nur im Import-Fluss** (analog Tagging). Der `POST /classify/rerun`-Endpoint (Bulk/Single-Neuberechnung mit Ledger-Reset, optional `caption_preset_id`) ist noch nicht gebaut — gehört in die Pipeline-Integration. Caption-Job-Bausteine liegen bereit: `enqueue_caption(asset_id, path)` + Ledger `caption_done`. *(Erledigt: `api/classify.py` + `rerun_job.py`)*
- [ ] → Phase 6: `model_registry.capabilities` (deklarativer UI-Descriptor §12.6) ist für Florence **noch nicht befüllt**; die Settings-UI-Steuerelemente (Task-Token-Dropdown etc.) sind Phase-6-Sache. Default-Preset ist aktuell **global** (`model_id = NULL`), nicht je-Captioner — bei mehreren Captionern in Phase 6 auf per-Modell-Default umstellen.
- [x] → Phase 4: Florence-Preprocessing wurde von 224er-CLIP-Center-Crop auf **768² Squash-Resize** korrigiert (`resize_squash` in `preprocessing.py`); CLIP/SigLIP (Phase 4) bleibt beim 224er Center-Crop — Funktionen sind getrennt, nicht versehentlich zusammenlegen. *(Erledigt: `preprocess_for_clip` bleibt 224er Center-Crop, nutzt jetzt aber `normalize_clip` mit CLIP-eigenen Stats statt ImageNet.)*
- [x] → Phase 5: `processing_ledger.embedding_done` existiert jetzt (Migration 0007). Rerun-Endpoint (`POST /classify/rerun`) muss diesen Flag für den `embedding`-Step zurücksetzen und `enqueue_embedding(asset_id, path)` neu anstoßen — Bausteine liegen bereit (`jobs/embedding_job.py`). *(Erledigt: `rerun_job.py` setzt alle Flags zurück und ruft `_run_embedding` direkt auf)*
- [x] → Phase 5: `vector_index.rebuild_index(session)` ist da, aber noch nirgends verdrahtet. Sinnvoller Aufhänger: ein Wartungs-/Rebuild-Target (`POST /api/maintenance/rebuild { target: 'embeddings' }`) oder Startup-Heilung — bei Phase 5/Pipeline-Integration entscheiden. *(Erledigt: `rebuild_job.py` + `maintenance.model.ts` erweitert auf `embeddings`)*
- [ ] → Phase 6: CLIP-Manifest nutzt jetzt HF-Snapshot (`Xenova/clip-vit-large-patch14`) → lädt **alle** onnx-Varianten (fp16/quantisiert) mit, da `_hf_snapshot_sync` nur bin/safetensors ignoriert. Falls Plattenplatz Thema wird, `snapshot_download` um `allow_patterns` erweitern (betrifft auch Florence). Der Adapter nimmt gezielt `onnx/vision_model.onnx` + `onnx/text_model.onnx`.
